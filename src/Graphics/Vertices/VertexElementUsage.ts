module es {
    /**
     * 定义顶点元素的用法
     */
    export enum VertexElementUsage {
        position,
        color,
        textureCoordinate,
        normal,
        binormal,
        tangent,
        blendIndices,
        blendWeight,
        depth,
        fog,
        pointSize,
        sample,
        tessellateFactor
    }
}