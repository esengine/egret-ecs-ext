module es {
    /**
     * 定义IndexBuffer和DynamicIndexBuffer中索引的大小
     */
    export enum IndexElementSize {
        /** 16bits */
        sixteenBits,
        /** 32bits */
        thirtyTwoBits,
    }
}